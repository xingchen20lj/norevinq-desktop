# Norevinq 名称初筛记录

最后更新：2026-08-14。

## 结论

产品采用自造词 **Norevinq**，不再使用带 `Code` 的产品名。该名称没有借用 OpenAI、DeepSeek、GitHub 或其他已知互联网产品的品牌构词、标志或视觉资产。

截至记录日期的公开网络初筛未发现同名软件、公司、GitHub 用户/仓库、npm 包、PyPI 包或 App Store 应用。Verisign `.com` RDAP 对 `norevinq.com` 返回未找到；这只表示查询时未看到注册记录，不是域名预留或所有权保证。

## 已做检查

- 搜索引擎对 `"Norevinq"`、`"Norevinq" software`、`"Norevinq" app` 和 `"Norevinq" trademark` 的精确检索；
- GitHub 用户接口与仓库名称精确检索；
- npm Registry 与 PyPI JSON 接口精确包名检索；
- Apple Search API 的应用名称精确匹配；
- Verisign `.com` RDAP 注册记录查询。

## 边界

这是工程阶段的名称排重，不是法律意见，也不能证明全球所有司法辖区都不存在在先权利。公开推广或申请商标前，应由专业代理至少检索中国第 9 类（可下载软件）与第 42 类（软件/技术服务），并按实际市场补充 WIPO、USPTO、EUIPO 等目标地区检索。

发现冲突时应在公开推广前更名；不要仅依赖域名、GitHub 账号或包名当前可用来判断商标安全。
